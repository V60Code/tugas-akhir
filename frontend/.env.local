# Frontend environment variables
# This file is NOT committed to git (add .env.local to .gitignore)

# URL of the FastAPI backend — change to your server URL in production
NEXT_PUBLIC_API_URL=http://localhost:8000
